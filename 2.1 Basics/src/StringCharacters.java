public class StringCharacters {	public static void main(String[] args) {		String text = "To be or not to be, that is the question;" + "Whether `tis nobler in the mind to suffer"				+ " the slings and arrows of outrageous fortune," + " or to take arms against a sea of troubles," + " and by opposing end them?";		int spaces = 0, vowels = 0, letters = 0;		// Loop through each letter in the text		for (int i = 0; i < text.length(); i++) {			// If the letter is a space			if (text.charAt(i) == ' ') {				// increment the number of spaces				spaces++;			} else {				// Increment the number of letters				letters++;				// Check if the letter is a vowel.				if (Character.toString(text.charAt(i)).toLowerCase().matches("[aeiou]")) {					// If it's a vowel increment the number of vowels.					vowels++;				}			}		}		System.out.println("The text contained: \n" + vowels + " vowels\n" + (letters - vowels)+ " consonants \n" + spaces  + " spaces." );	}}