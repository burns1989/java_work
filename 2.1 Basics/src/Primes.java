public class Primes {	public static void main(String[] args) {		int nValues = 50;		// Loops through the numbers 2 to nValues		loop: for (int i = 2; i <= nValues; i++) {			// Loops through the numbers 2 to the square root of i			for (int j = 2; j <= Math.sqrt(i); j++) {				// If i divides evenly by j it's not a prime so continue to the next number				if (i % j == 0) {					continue loop;				}			}			System.out.println(i);		}	}}